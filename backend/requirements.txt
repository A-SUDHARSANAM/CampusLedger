# FastAPI & ASGI
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Supabase
supabase==2.10.0

# Auth & Security
python-jose[cryptography]==3.3.0

# Settings
pydantic<3.0
pydantic-settings>=2.5.0
pydantic[email]<3.0

# Utilities
python-multipart==0.0.20
python-dotenv==1.0.1

# QR Code generation
qrcode[pil]==8.0

# OCR — invoice scanning
# Requires Tesseract binary: https://github.com/UB-Mannheim/tesseract/wiki
pytesseract==0.3.13
Pillow>=10.0.0
# OpenCV for image preprocessing (grayscale + threshold)
opencv-python>=4.9.0
# For OpenAI GPT-4 Vision engine (optional, higher accuracy)
openai>=1.51.0

# ── ML — Inventory Demand Prediction ─────────────────────────────────────────
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
joblib>=1.3.0          # bundled with scikit-learn; listed explicitly for clarity

# Database access for training scripts (direct PostgreSQL via SQLAlchemy)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL driver

# Optional — automatic monthly model retraining scheduler
# apscheduler>=3.10.0

